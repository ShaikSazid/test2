name: Test Workflow

on:
    push:
        branches:
            - main

jobs:
    test:
        runs-on: ubuntu-latest

        steps:
            - name: Print message
              run: echo "Github actions is working!!"