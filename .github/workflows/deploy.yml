name: Test Workflow

on:
    push:
        branches:
            - main

jobs:
    test:
        runs-on: ubuntu-latest

        steps:
            # 1. Checkout your code
            - name: Checkout code
              uses: actions/checkout@v3

            # 2. Print Message
            - name: Print message
              run: echo "Code checkout sucessful"